<%= content_for(:title) do %>
  <%= t(".title") %>
<% end %>

<div class="flex justify-center mt-10 flex-col items-center gap-4">
  <div class="container mx-auto px-[12px] md:px-24 xl:px-12 max-w-[1300px] nanum2">
    <div class="grid mt-4 grid-cols-1 lg:grid-cols-2 xl:grid-cols-3 gap-x-4 gap-y-28 lg:gap-y-16">
      <%= render @listings %>
    </div>
  </div>
  <div class="px-6 py-4 grid gap-3 md:flex md:justify-between md:items-center">
    <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
  </div>
</div>
