  <% flash.each do |level, message| %>
    <div class="<%= flash_class(level) %> notification" data-turbo-cache="false">
      <span class="block sm:inline"><%= message %></span>
    </div>
  <% end %>
